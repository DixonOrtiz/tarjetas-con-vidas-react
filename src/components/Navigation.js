import React from 'react';

function Navigation() {
    return(
        <nav className="navbar navbar-dark bg-primary">
            <span className="navbar-brand mb-0 h1">Tarjetas con vidas</span>
        </nav>
    );
} 

export default Navigation;